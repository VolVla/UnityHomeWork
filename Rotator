using UnityEngine;

public class Rotator : MonoBehaviour
{
    [SerializeField] float _speed = 1;

    private void Update()
    {
        Spin();
    }

    private void Spin()
    {
        transform.Rotate(0, _speed, 0);
    }
}
